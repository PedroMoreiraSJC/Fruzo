const botao = document.getElementById("alerta-botao");
botao.addEventListener("click", function () {
    alert("Entre em contato pelo nosso WhatsApp,");
});

window.addEventListener('scroll', function() {
    window.scrollTo(0, window.scrollY);
});
